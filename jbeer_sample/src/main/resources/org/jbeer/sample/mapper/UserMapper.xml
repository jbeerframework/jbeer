<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jbeer.sample.mapper.UserMapper">
	 <insert id="insert" useGeneratedKeys="true" parameterType="User" keyProperty="id">
	 	insert into user (userName,userAge,createTime) values(#{userName},#{userAge},#{createTime})
	 </insert>
</mapper>